[build.environment]
# NEXT_PUBLIC_* vars are intentionally bundled to the client (Next.js convention).
# EMAIL_FROM / REPLY_TO_EMAIL are addresses, not secrets, and have matching default fallbacks in code.
SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_URL,NEXT_PUBLIC_SUPABASE_ANON_KEY,NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY,NEXT_PUBLIC_GA_ID,NEXT_PUBLIC_META_PIXEL_ID,NEXT_PUBLIC_MAILCHIMP_URL,NEXT_PUBLIC_COMING_SOON,EMAIL_FROM,REPLY_TO_EMAIL,CONTACT_EMAIL"
